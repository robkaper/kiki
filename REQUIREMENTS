Kiki was written to be used in environments where users have access to and
control over:

- the webserver configuration. Historically Apache, but instructions will be
  updated for nginx (and possibly solely nginx).

- a root structure outside of the actual document root (typically htdocs/).

Old notes:

Facebook PHP SDK >= 3.1.1
https://github.com/facebook/facebook-php-sdk

Optional, but required for Facebook logins and connectivity.

Install path must be configured as: Config::$facebookSdkPath

----------------------------------------------------------------------------

TwitterOAuth SDK
https://github.com/abraham/twitteroauth/

Optional, but required for Twitter logins and connectivity.

Install path must be configured as: Config::$twitterOAuthPath
